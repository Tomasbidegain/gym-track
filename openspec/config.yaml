schema: spec-driven

context: |
  Tech stack: React Native 0.81.5 + Expo SDK ~54.0.0 + TypeScript ~5.8.0 + Firebase ^12.14.0
  Architecture: Clean / Hexagonal (domain / data / presentation layers)
  Testing: No test runner or test files detected
  Style: Functional components, React Context + custom hooks, Spanish UI labels

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns (use cases, repositories, hooks, screens)
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: "npx expo start --no-dev --minify"
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas

testing:
  strict_tdd: false
  runner: none
  unit: false
  integration: false
  e2e: false
  coverage: false
  linter: false
  type_checker: true
  formatter: false
